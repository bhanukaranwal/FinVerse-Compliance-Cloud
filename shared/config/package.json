{
  "name": "@finverse/shared-config",
  "version": "1.0.0",
  "description": "Shared configuration for FinVerse Compliance Cloud",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "joi": "^17.9.2"
  },
  "devDependencies": {
    "typescript": "^5.1.6",
    "@types/node": "^20.5.0"
  }
}
